import React from "react";
import { View, Text } from "react-native";

const SoilQuality: React.FC = () => {
  return (
    <View className="bg-green-100 p-4 rounded-lg">
      <Text className="text-lg font-bold">Soil Quality</Text>
      {/* Add soil quality data here */}
    </View>
  );
};

export default SoilQuality;
